'use client';
export default function SettingsPage(){
  return (
    <div style={{padding:24}}>
      <h2>설정 / 키 입력</h2>
      <p>이미 백엔드 DB에 저장된 키를 사용합니다. 프론트에서 추가 입력 폼이 필요한 경우 이후 확장 가능합니다.</p>
    </div>
  )
}
